PROJECT(B)
    SET(SOURCES b.cpp)
    INCLUDE_DIRECTORIES(${CMAKE_INSTALL_DIR}/include)
    ADD_LIBRARY(b b.cpp)
    INSTALL(TARGETS b DESTINATION lib)
    INSTALL(FILES b.h DESTINATION include)
ENABLE_TESTING()
